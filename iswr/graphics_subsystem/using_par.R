help("par") # parameters page, line width, type, character size
# font, colour, style, axis calculation, size of plot and figure
# regions, clipping, etc

par(mfrow)
par(mfcol)

# change default margin size from 5, 4, 4, 2
# before plotting
par(mar=c(4,4,2,2) + 0.1) # shaves 1 line off of the top and bottom
